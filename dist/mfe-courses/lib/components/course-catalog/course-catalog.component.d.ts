import { OnInit } from '@angular/core';
import { FormBuilder, FormGroup } from '@angular/forms';
import { MatDialog } from '@angular/material/dialog';
import { MatSnackBar } from '@angular/material/snack-bar';
import { PageEvent } from '@angular/material/paginator';
import { Router } from '@angular/router';
import { Course } from 'shared-lib';
import { Category } from 'shared-lib';
import { Tag } from 'shared-lib';
import { CourseService } from 'shared-lib';
import { CategoryService } from 'shared-lib';
import { TagService } from 'shared-lib';
import { AuthService } from 'shared-lib';
import * as i0 from "@angular/core";
export declare class CourseCatalogComponent implements OnInit {
    private courseService;
    private categoryService;
    private tagService;
    private authService;
    private fb;
    private snackBar;
    private router;
    private dialog;
    courses: Course[];
    categories: Category[];
    tags: Tag[];
    languages: string[];
    uniqueLanguages: Set<string>;
    searchForm: FormGroup;
    isLoading: boolean;
    totalCourses: number;
    pageSize: number;
    currentPage: number;
    pageSizeOptions: number[];
    companyId: number;
    userRole: string;
    constructor(courseService: CourseService, categoryService: CategoryService, tagService: TagService, authService: AuthService, fb: FormBuilder, snackBar: MatSnackBar, router: Router, dialog: MatDialog);
    ngOnInit(): void;
    private initForm;
    private loadCurrentUser;
    private loadFilterData;
    searchCourses(): void;
    onPageChange(event: PageEvent): void;
    onFilterChange(): void;
    resetFilters(): void;
    viewCourse(courseId: number): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<CourseCatalogComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<CourseCatalogComponent, "app-course-catalog", never, {}, {}, never, never, false, never>;
}
