import { OnInit } from '@angular/core';
import { FormBuilder, FormGroup, FormArray, AbstractControl } from '@angular/forms';
import { ActivatedRoute, Router } from '@angular/router';
import { MatSnackBar } from '@angular/material/snack-bar';
import { Category } from 'shared-lib';
import { Tag } from 'shared-lib';
import { Course } from 'shared-lib';
import { CourseService } from 'shared-lib';
import { CategoryService } from 'shared-lib';
import { TagService } from 'shared-lib';
import { MediaService } from 'shared-lib';
import { CourseContentService } from 'shared-lib';
import { Media } from 'shared-lib';
import { CourseContent } from 'shared-lib';
import * as i0 from "@angular/core";
export declare class CourseCreateComponent implements OnInit {
    private fb;
    private courseService;
    private categoryService;
    private tagService;
    private mediaService;
    private courseContentService;
    private router;
    private snackBar;
    private route;
    courseForm: FormGroup;
    categories: Category[];
    tags: Tag[];
    isLoading: boolean;
    isEditMode: boolean;
    courseId?: number;
    existingCourse?: Course;
    existingMedia?: Media;
    existingContents: CourseContent[];
    uploadedFile: File | null;
    previewUrl: string | null;
    courseMediaFile: File | null;
    contentMediaFiles: Map<number, File>;
    constructor(fb: FormBuilder, courseService: CourseService, categoryService: CategoryService, tagService: TagService, mediaService: MediaService, courseContentService: CourseContentService, router: Router, snackBar: MatSnackBar, route: ActivatedRoute);
    ngOnInit(): void;
    initForm(): void;
    loadCourseData(courseId: number): void;
    populateForm(): void;
    loadCategories(): void;
    loadTags(): void;
    get contents(): FormArray;
    get media(): FormGroup;
    getMediaGroup(content: AbstractControl): FormGroup;
    addContent(): void;
    removeContent(index: number): void;
    onSaveCourse(): Promise<void>;
    onDiscardCourse(): void;
    onFileSelected(event: Event, contentIndex?: number): void;
    isImageFile(contentIndex?: number): boolean;
    isVideoFile(contentIndex?: number): boolean;
    getMediaUrl(url: string | undefined): string;
    static ɵfac: i0.ɵɵFactoryDeclaration<CourseCreateComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<CourseCreateComponent, "app-course-create", never, {}, {}, never, never, false, never>;
}
