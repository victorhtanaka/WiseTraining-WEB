import { OnInit } from '@angular/core';
import { FormBuilder, FormGroup } from '@angular/forms';
import { MatDialog } from '@angular/material/dialog';
import { MatSnackBar } from '@angular/material/snack-bar';
import { Company } from 'shared-lib';
import { User } from 'shared-lib';
import { Group } from 'shared-lib';
import { CompanyManagementService } from 'shared-lib';
import { GroupService } from 'shared-lib';
import * as i0 from "@angular/core";
export declare class CompanyManagementComponent implements OnInit {
    private companyManagementService;
    private groupService;
    private fb;
    private snackBar;
    private dialog;
    company: Company;
    users: User[];
    groups: Group[];
    companyForm: FormGroup;
    isLoading: boolean;
    activeTab: number;
    constructor(companyManagementService: CompanyManagementService, groupService: GroupService, fb: FormBuilder, snackBar: MatSnackBar, dialog: MatDialog);
    ngOnInit(): void;
    initForm(): void;
    loadCompanyData(): void;
    loadCompanyUsers(): void;
    loadCompanyGroups(): void;
    saveCompany(): void;
    openAddUserDialog(group: Group): void;
    addUserToGroup(userId: number, groupId: number): void;
    removeUserFromGroup(userId: number, groupId: number): void;
    getUserFullName(userId: number): string;
    changeTab(tabIndex: number): void;
    openGroupDialog(group?: Group): void;
    createGroup(group: Group): void;
    updateGroup(group: Group): void;
    confirmDeleteGroup(group: Group): void;
    deleteGroup(groupId: number): void;
    isAdminUserInAdminGroup(userId: number, groupId: number): boolean;
    getCurrentUserId(): number;
    confirmRemoveUserFromGroup(user: User, group: Group): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<CompanyManagementComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<CompanyManagementComponent, "app-company-management", never, {}, {}, never, never, false, never>;
}
